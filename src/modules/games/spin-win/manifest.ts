import type { GameManifest } from "@/contracts/game";

export const spinWinManifest: GameManifest = {
  id: "spin-win",
  name: "Spin & Win",
  icon: "🎡",
  enabled: true,
  supportsGamePk: false,
};
